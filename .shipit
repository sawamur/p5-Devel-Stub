#steps = FindVersion, ChangeVersion, CheckChangeLog, DistTest, Commit, Tag, MakeDist, UploadCPAN
steps = FindVersion,DistTest,MakeDist, UploadCPAN
git.tagpattern = p5-Devel-Stub-%v
git.push_to = origin

